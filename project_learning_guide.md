# NAS-Tools 源码学习指南 (写给 Python 初学者)

欢迎！如果你刚学完基本的 Python 语法（如变量、函数、类、字典），这篇指南将带你一步步看懂 NAS-Tools 这个“庞然大物”。

## 1. 这是一个什么样的项目？

NAS-Tools 是一个 **PT 资源搜索与下载管理工具**。它的核心工作流程如下：
1. **资源搜索**：通过内置索引器在已配置的 PT 站点聚合搜索种子资源。
2. **刷流做种**：根据规则自动下载免费种子赚取上传量（刷流功能）。
3. **识别并选择路径**：通过 TMDB 识别资源类型和二级分类，再按配置与磁盘可用空间选择下载目录。
4. **推送到下载器**：通过 API 告诉 qBittorrent 或 Transmission 开始下载，并管理任务状态、通知和自动删种。
5. **运行内置工具**：自动完成站点签到、保号登录等独立任务，并复用项目已有的站点、调度、数据库和通知能力。

为了实现这些，项目不仅需要大量的**后台死循环逻辑**，还需要一个 **Web 网页后台** 来给你提供点击、配置的界面。

## 2. 项目目录结构总览

不用去看所有的文件，只要明白这几个核心大类即可：

```text
nas-tools/
├── run.py                 <-- 【必看入口】Gunicorn worker 加载时执行的应用初始化入口
├── config.py              <-- 【必看基础】加载和管理 /config/config.yaml
├── config/                <-- 镜像内置的配置模板与站点数据；用户数据在 Docker /config 卷中
├── app/                   <-- 【核心业务区】所有后台默默干活的代码都在这里
│   ├── scheduler.py       <-- 【大管家】定时任务调度，规定了谁几点钟干什么活
│   ├── brushtask.py       <-- 刷流任务的具体逻辑
│   ├── searcher.py        <-- 调度内置索引器聚合搜索资源
│   ├── media/             <-- 媒体识别，负责查询 TMDB 并生成类型和二级分类
│   ├── downloader/        <-- 下载器控制台，对接 qBittorrent 和 Transmission API
│   ├── sites/             <-- 管理站点连接、Cookie 和用户数据
│   ├── tools/             <-- 面向用户的内置工具，使用静态注册表统一登记
│   └── utils/             <-- 通用代码，像 http_utils.py（发网络请求）都在这里
├── web/                   <-- 【Web 页面区】Flask 后端框架代码
│   ├── main.py            <-- Web 服务器的启动配置、拦截器配置
│   ├── apiv1.py           <-- 后端接口，负责接手前端页面发来的 JSON 请求并干活
│   ├── action.py          <-- 相当于 apiv1.py 的小弟，具体活儿他来干
│   ├── routes/            <-- Blueprint 页面路由，包含工具聚合页和工具页面
│   ├── tools/             <-- 把内置工具适配到 Web 动作协议
│   ├── templates/         <-- 所有的 HTML 页面
│   └── static/            <-- 所有的 CSS 样式、JS 脚本、图片
└── check_config.py        <-- 检查用户瞎填配置文件的校验脚本
```

## 3. 学习路径推荐 (循序渐进)

为了不被代码淹没，请**严格按照以下顺序**阅读代码！我已经在这几个文件中加上了非常详细的中文注释。

### 第一关：程序是如何启动的？

先看 **`docker/entrypoint.sh`** 和 **`docker/gunicorn.conf.py`**，再阅读 **`run.py`**。

- Docker 入口先切换到配置的 UID/GID，再由 Gunicorn 加载 `run:App`。
- `run.py` 负责初始化配置和数据库，然后启动定时任务等后台服务；Web 请求由 Gunicorn 承载。

### 第二关：它是怎么读配置的？

请打开阅读 **`config.py`**。

- 了解单例模式 (`Singleton`)：如何确保全系统只有一个配置管家。
- 看它是怎么用 `yaml` 库把文本读取成 Python 字典的。

### 第三关：后台任务是怎么定时执行的？

请打开阅读 **`app/scheduler.py`**。

- 了解 `APScheduler` 定时任务库。
- 看看它是怎么安排元数据缓存、站点数据刷新，以及调用工具模块执行自动签到等任务的。

### 第四关：内置工具是怎么接入的？

先看 **`app/tools/registry.py`**，再阅读 **`app/tools/site_signin/service.py`**。

- `registry.py` 使用稳定 ID 显式登记工具的名称、页面、权限和展示顺序，供 `/tools` 聚合页筛选和展示；它不是插件加载器，也不统一启动工具。
- `site_signin/service.py` 负责签到和保号登录的执行编排，但站点与 Cookie 来自 `app/sites/`，历史写入现有数据库，定时执行和通知也复用项目公共能力。
- `app/tools/` 放有独立业务入口、页面或运行状态的工具，`app/utils/` 放可被多个模块调用的通用函数，不要混淆这两个目录。
- 从 MoviePilot 插件库引入能力时，需要先改造成上述项目边界，再静态注册；不能把 MoviePilot 插件目录直接复制进来运行。

如果一个工具来自外部项目，还可以在注册表中看到它的上游仓库、源码路径、固定 revision、作者和许可证。这些信息用于核对来源以及判断后续如何同步升级。

### 第五关：一个具体业务是怎么跑的？（以刷流为例）

请打开阅读 **`app/brushtask.py`**。

- 它是怎么查出你想要刷哪个站的？
- 怎么读取站点的刷流 RSS，并按规则找出合适的免费种子？
- 最后又是怎么把种子塞给 qBittorrent 的？

### 第六关：添加下载时，目录是怎么自动选择的？

请打开阅读 **`app/downloader/downloader.py`**。

- `download()` 在未显式指定目录时会调用下载目录匹配逻辑。
- 下载目录规则按配置顺序匹配媒体类型和二级分类，并通过容器访问路径检查可用空间。
- 匹配成功后把保存路径和可选分类标签传递给下载器客户端。

### 第七关：程序是怎么控制外部下载器的？

请打开阅读 **`app/downloader/client/qbittorrent.py`**。

- 你将学到如何继承基础类 `_BaseClient`。
- 学会用第三方的 API 库（如 `qbittorrent-api`）来控制外部软件。

### 第八关：前端页面是怎么拿到数据的？

请依次查看 **`web/routes/tool_pages.py`**、**`web/templates/tools/`**、**`web/tools/`** 和 **`web/apiv1.py`**。

- 学习著名的 Web 框架 **Flask** 的基本用法（路由 `@app.route` 是什么，Blueprint 蓝图是什么）。
- 从工具注册表中的 `page` 找到对应页面路由和 Jinja 模板，观察 `web/tools/` 如何通过统一动作接口读取状态、启动后台任务。

---

> 💡 **给初学者的提示：**
> 如果你在代码里看到了诸如 `@staticmethod`、`*args, **kwargs`、`yield`、或者不认识的第三方库，别害怕！你只要先大概猜出这行代码“想做什么”即可，不必深究它是“怎么做到的”。当你理清了所有业务流程后，再挑选一两个感兴趣的细节去查文档，这就是最高效的代码阅读方式！
